{% macro card(params) %}
<article class="card">
	<a class="link" href="{{ params.url }}" target="_blank">
		<h3>{{ params.title }}</h3>
		<span>Check it out <span class="arrow arrow--external">→</span></span>
	</a>
</article>
{% endmacro %}
