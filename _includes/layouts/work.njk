---
layout: layouts/base.njk
---

<main>
	<article>
		<h1>{{ title }}</h1>
		{{ content | safe }}
		{% if projectUrl %}
		<a href="{{ projectUrl }}">
			View {{ 'Website' if tags == 'web' else 'Case Study' }} Here
			<span>→</span>
		</a>
		{% endif %}
	</article>
</main>
